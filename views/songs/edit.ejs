<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>Musify</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>Edit Song</h1>

<form action="/songs/<%= song._id %>?_method=PUT" method="POST">

  <label for="title">Title</label>
  <input type="text" name="title" id="title" value="<%= song.title %>" required/>

  <label for="artist">Artist</label>
  <input type="text" name="artist" id="artist" value="<%= song.artist.name || song.artist %>" required/>

  <label for="genre">Genre</label>
  <input type="text" name="genre" id="genre" value="<%= song.genre.name || song.genre %>" required/>
    <button type="submit">Update Song</button>
</form>
</body>
</html>